swap:
  deployment:
    name: payroll
    image: gradle:jdk
    command:
    - tail
    - -f
    - /dev/null
mount:
  source: .
  target: /usr/src/app
scripts:
  hello: echo Your cluster ♥ you
  build: gradle build -continuous --info --scan
  watch: gradle bootRun
