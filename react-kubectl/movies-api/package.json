{
  "dependencies": {
    "express": "^4.16.4",
    "nodemon": "^1.18.9"
  },
  "scripts": {
    "start": "yarn install && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
